<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= root_path%>">Task Management System</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
  </button>
  
  <div class="navbar-collapse collapse" id="navbarSupportedContent">
    <% if current_user %>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item nav-link active">
          <strong><%= link_to "Dashboard", user_dashboard_path, class:"nav-link text-center"%></strong>
        </li>
        <li class="nav-item nav-link">
            <strong><%= link_to "My Task", tasks_path, class:"nav-link text-center"%></strong>
        </li>
        <li class="nav-item btn-group" data-behavior="notifications">
          <a class="dropdown-toggle nav-link" data-behavior="notifications-link" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-bell"></i><span class="red" id="unread-count">(<%= (Notification.where(recipient: current_user).unread).count %>)</span>
          </a>
          <div class="dropdown-menu notification-menu dropdown-menu-right" aria-labelledby="dropdownMenu1">
            <div class="dropdown-header">
              <div class="row">
                <div class="col-12 col-sm-6">
                  <h5>Notification
                    <span class="red" id="unread-count1">(
                      <%= (Notification.where(recipient: current_user).unread).count %>)
                    </span>
                  </h5>
                </div>
                <div class="col-12 col-sm-3">
                  <h6 class="text-right">
                    <%= link_to "Mark all as read", mark_all_read_notifications_path, method:"post", class:""%>
                  </h6>
                </div>
                <div class="col-12 col-sm-3">
                  <h6 class="text-right">
                    <%= link_to "See all",notifications_path%>
                  </h6>
                </div>
              </div>
            </div>
            <div class="dropdown-divider"></div>
            <div id="notifications">
              <%= render "layouts/notification" %>
            </div>
          </div>
        </li>
        <li class="nav-item nav-link">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <strong><%= current_user.name %></strong>
              </a>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <%= link_to  "Sign out", logout_path, method: :delete, class: "dropdown-item" %>
              </div>
            </li>
          </ul>
        </li>
      </ul>
    <% else%> 
      <% unless["new"].include?(params[:action])%>
        <ul class="navbar-nav ml-auto">
          <li class="nav-link text-center">
            <%= link_to "Admin Login", admin_login_path, class:"btn btn-outline-success my-2 my-sm-0" %>
          </li>
        </ul>
      <% end %>
    <% end %>
  </div>
</nav>

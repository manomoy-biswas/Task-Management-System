<% content_for :title, "Assigned by me" %>
<div class="row justify-content-center">
  <div class="col-12 col-xl-10">
    <div class="white-box bg-light">
      <div class="task-list">
        <div class="row justify-content-end">
          <div class="col-12 col-sm-8 col-md-5 col-lg-4 col-xl-3">
            <%=form_tag(user_assigned_task_tasks_path, action:"assign_by_me", method: :get) do %>
              <div class="form-group row">
                <% priorities=["Low", "Medium", "High"]%>
                <span class="fa fa-filter col-2 text-right"></span>
                <div class="col-10 text-center">
                  <%= select_tag(:priority, options_for_select(priorities, params[:priority]), prompt:"All", class:"form-control text-center select-center", id:"filter_input") %>
                </div>
              </div>  
            <% end %>
          </div>
        </div>
        <div class="index-table">
          <%= render "tasks/task_list"%>
        </div>
      </div>
    </div>
  </div>
</div>
